global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'localcompute-coordinator'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['coordinator:8000']
